<div
    id="umb-member-impersonation-modal"
    class="umb-member-impersonation-modal"
    popover
    data-modal>
    <div class="umb-member-impersonation-modal-content">
        <h1>Members</h1>
        <input name="search" type="text" placeholder="Search" />
        <div class="umb-member-impersonation-action-items">
            <button
                type="button"
                onclick="this.closest('[popover]').hidePopover()"
                aria-label="Close"
                class="umb-member-impersonation-btn umb-member-impersonation-btn-close">
                <span class="label">Close</span>
            </button>
            <button
                type="button"
                onclick="this.closest('[popover]').hidePopover()"
                aria-label="Close"
                class="umb-member-impersonation-btn umb-member-impersonation-btn-impersonate">
                <span class="label">Impersonate</span>
            </button>
        </div>
    </div>
    <div>
    </div>
</div>

<style>
    .umb-member-impersonation-modal {
        position: relative;
        margin: auto;
        max-height: 90vh;
        min-width: 600px;
        max-width: 90vw;
        overscroll-behavior: contain;
        border: 0;
        border-radius: 1rem;
        background-color: white;
        padding: 30px 42px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        color: #000;
    }
    .umb-member-impersonation-modal::backdrop {
        backdrop-filter: blur(8px) brightness(0.8);
    }
    .umb-member-impersonation-btn {
        background: none;
        color: inherit;
        border: 1px solid inherit;
        padding: 0.375rem 1.125rem;
        font: inherit;
        cursor: pointer;
        outline: inherit;
        border-radius: 3px;
    }
    .umb-member-impersonation-btn-close:hover {
        color: #3544b1;
        background: #fafafa;
    }
    .umb-member-impersonation-btn-impersonate {
        background: #0d8152;
        color: #fff;
    }
    .umb-member-impersonation-btn-impersonate:disabled {
        background: rgb(243, 243, 245);
        color: rgb(196, 196, 196);
    }
    .umb-member-impersonation-modal-content {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
        align-items: center;
    }
    .umb-member-impersonation-action-items {
        width: 100%;
        display: flex;
        flex-direction: row;
        gap: 0.25rem;
        justify-content: end;
    }
</style>
